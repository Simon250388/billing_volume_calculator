spring:
  datasource:
    url: jdbc:h2:mem:accounting_point;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS public
    username: test
    password: test

  liquibase:
    change-log: classpath:db/db.changelog-master.xml


